<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="StudentPartners.Views.ProfilePage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:StudentPartners.Controls;"
             Title="Profile">
	<ContentPage.ToolbarItems>
		<ToolbarItem Name="Edit" x:Name="editProfile"></ToolbarItem>
	</ContentPage.ToolbarItems>
	<ScrollView>
		<StackLayout BackgroundColor="White">
        	<local:CardView Detail="{Binding Country}"
                        ImageSource="{Binding ProfilePhotoUrl}"
                        Text="{Binding Name}" />
      		<StackLayout Padding="10,10,10,10">
        		<Label Text="Biography" FontSize="Large" TextColor="#030303" />
        		<Label Text="{Binding Biography}" />
      		</StackLayout>
  		</StackLayout>
	</ScrollView>
</ContentPage>